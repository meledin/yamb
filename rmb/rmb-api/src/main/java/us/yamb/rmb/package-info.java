/**
 * RMB is a REST-based messaging abstraction with support for <i>bi-directional REST messages</i>. It makes no distinction
 * between traditional "clients" and "servers" but allows each to act as both. RMB is an abstraction layer, and thus works over
 * many different underlying technologies, although it will work best with a message bus.
 * <p>
 * RMB is instantiated using the {@link us.yamb.rmb.builders.RMBBuilder} class, as follows
 * <pre>
 * </pre>
 * 
 * @author Vladimir Katardjiev
 */
package us.yamb.rmb;